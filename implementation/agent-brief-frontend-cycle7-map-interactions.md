# Agent Brief — FE Lane (Cycle 7 Map Interaction Modes)

Owner: Frontend lane

## Mission
Expose map interaction selection on landing and preserve round/final flow UX.

## Files (owned)
- `src/screens/LandingScreen.tsx`
- `src/screens/RoundScreen.tsx`
- `src/components/NavControls.tsx`
- `src/hooks/useGameState.ts`
- `src/App.tsx`

## Tasks
1. Add Option 1/2/3 selector UI to landing screen.
2. Start game with selected interaction mode.
3. Pass selected mode into round screen and UI controls.
4. Keep 10-round progression unchanged.

## Verification
- `npm run build`
- Manual check: selector works and selected mode is visible/active in round HUD.

## Closeout format
STATUS + files changed + verification result + blockers.
